name: 'uk_financial_lakehouse'
version: '1.0.0'
config-version: 2

profile: 'uk_financial_lakehouse'

model-paths: ["models"]
test-paths: ["tests"]
log-path: "logs"
clean-targets: ["target", "dbt_packages"]

models:
  uk_financial_lakehouse:
    bronze:
      +materialized: view
    silver:
      +materialized: view
    gold:
      +materialized: table
